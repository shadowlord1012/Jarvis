# repos

This is the first attempt at making a working Jarvis style AI Assistant written in C# using WPF as an entry point. 

Ollama -> LLM
Edge -> STT (Does have the workings with Eleven Labs with API Key)
Whisper - > TTS
Maria DB -> Working on getting it to have its own Database for Queries to lessen the load when accessing the LLM (Helps
              Reduce the number of tokens if the question that is being asked.)
100% LOCAL Can use the Eleven Labs API id needed but not required.

This is a work in progress and has a lot of bugs, minimal tools, but does have most basic functionality.
